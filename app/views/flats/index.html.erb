<h1>Flats</h1>
<ul>
  <% @flats.each do |flat| %>
    <li>
      <h3><%= flat["name"] %></h3>
      <p>💰 <%= flat["price"] %> <%= flat["priceCurrency"] %></p>
      <%= link_to "See more", flat_path(flat["id"]) %>
    </li>
  <% end %>
</ul>
